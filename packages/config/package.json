{
  "name": "@versa-chat/config",
  "version": "0.0.1",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "clean": "rimraf .turbo && rimraf node_modules && rimraf dist && rimraf tsconfig.tsbuildinfo",
    "dev": "tsc --build --watch",
    "build": "tsc --build"
  },
  "dependencies": {
  },
  "devDependencies": {
    "typescript": "^4.9.0"
  }
}
